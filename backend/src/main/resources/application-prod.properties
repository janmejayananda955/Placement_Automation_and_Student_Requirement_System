spring.application.name=PlacementAutomationandStudentRequirementSystem

spring.datasource.url=jdbc:postgresql://${PGHOST+PGDATABASE}:5432/${PGDATABASE}?${PGSSLMODE}
spring.datasource.username=${PGUSER}
spring.datasource.password=${PGPASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.servlet.context-path=${CONTEXT-PATH}

debug=true

jwt.secretKey=${JWT_SECRET_KEY}
jwt.tokenDuration=${JWT_TOKEN_DURATION}

server.port=${PORT:8080}

cross.allowedOrigins=${ALLOWED_ORIGINS}
